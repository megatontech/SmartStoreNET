@model ProductSummaryModel

@using SmartStore.Web.Models.Catalog;

@{ 
	if (!Model.Items.Any())
	{
		return;
	}
}

<div class="pd-related-products block block-boxed mb-3 panel-box p-2">
    <div class="heading">
        <h3 class="heading-title fs-h1">@T("Products.RelatedProducts")</h3>
    </div>
    <div class="block-body pt-0">
		<div class="artlist-carousel">
			@{
				Html.RenderPartial("Product.List", Model);
			}
		</div>
    </div>
</div>