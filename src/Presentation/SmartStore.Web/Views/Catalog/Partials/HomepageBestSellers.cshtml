@model ProductSummaryModel

@using SmartStore.Web.Models.Catalog;

@{ 
    if (Model.Items.Count == 0)
    {
        return;
    }
    Model.Items = Model.Items.Take(2).ToList();
}
    <div class="product-grid bestsellers block" style="background: #fff; border-color: #ddd; box-shadow: 0 0 3px rgba(0, 0, 0, 0.1); margin: 0 0.5rem 1rem 0.5rem; -webkit-border-radius: 1em; -moz-border-radius: 1em; -o-border-radius: 1em; -ms-border-radius: 1em;  border-radius: 1em; ">
        <div class="heading heading-center" style="        background: none; text-align: left; left: 5px;
">
            <h3 class="heading-title fs-h1" style="color: #c51d1d; font-weight: bold; font-family: fantasy; ">
                <b style=" font-size: 25px; "><i class="icm icm-bag"></i>&nbsp; @T("Bestsellers") </b>
            </h3>
            <div style="width: 35%;display: inline-block;"></div>
            <h5 style=" display: inline-block; ">更多 &gt;</h5>
        </div>
        @*<div class="product-grid bestsellers block">
            <div class="heading heading-center">
                <h3 class="heading-title fs-h1">@T("Bestsellers")</h3>
            </div>*@
            <div class="block-body pt-3 artlist-no-separators">
                @{
                    Html.RenderPartial("Product.List", Model);
                }
            </div>
        </div>
